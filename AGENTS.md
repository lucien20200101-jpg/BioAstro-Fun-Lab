# AGENTS.md

## 项目名称
BioAstro Fun Lab

## 项目定位
生命与宇宙趣味科学工具箱。它是一个可自我扩展的 H5 网页应用，包含生物学、天文学以及生命与宇宙交叉方向的趣味工具。

## 技术栈
- React
- Vite
- TypeScript
- Tailwind CSS
- React Router
- localStorage
- 本地 JSON 内容库

## 长期开发规则
1. 所有知识内容必须数据化。
2. 不允许把冷知识、题目、测试结果、细胞器角色、星座资料、星球资料、剧情节点、评分规则硬编码在 React 组件中。
3. 前台页面只负责读取数据并渲染。
4. 用户修改后的内容保存到 localStorage。
5. 前台读取数据时，localStorage 优先于默认 JSON。
6. 所有数据集都要支持导入、导出、恢复默认。
7. 第一版不做登录系统、云数据库、AI API、后端服务和微信小程序。
8. 第一版不要强依赖外部 API。
9. UI 风格为深蓝宇宙背景、生命绿、生物蓝、星云紫、卡片式布局、轻量微交互。
10. 优先适配手机端，同时兼容桌面端。
11. 每轮修改前先检查现有结构，避免重复造组件。
12. 每轮完成后必须尽量运行：
   - npm install
   - npm run build
13. 如果项目配置了 lint，也运行：
   - npm run lint
14. 生成补丁前自行检查：
   - merge conflicts
   - 重复定义
   - 路由冲突
   - 组件命名冲突
   - 类型定义冲突
   - 导入路径错误
   - TypeScript 报错
   - 构建失败
15. 不要引入大型 UI 库。
16. 不要引入外部字体。
17. 不要破坏已有功能。
18. 不要删除用户已有文件，除非明确确认它是废弃文件。
19. 每次任务完成后输出：
   - 修改摘要
   - 修改文件清单
   - 已运行的检查命令
   - 检查结果
   - 尚未完成的问题
   - 下一步建议

## Review guidelines
- 重点检查硬编码内容、localStorage 覆盖逻辑、JSON 导入导出、路由刷新、移动端布局和构建错误。
- 对科学内容保持谨慎表达，不要把“可能存在生命”写成“必然存在生命”。
