# BioAstro-Fun-Lab

## Deployment (Vercel)
- Build Command: `npm run build`
- Output Directory: `dist`
- SPA routing fallback is configured in `vercel.json` to rewrite all routes to `index.html` for React Router refresh support.

For detailed steps and route verification checklist, see `docs/deployment.md`.
